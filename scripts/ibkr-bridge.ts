/**
 * IBKR Bridge — DEPRECATED
 *
 * This project now uses the OANDA bridge for live trading.
 * Run: npm run oanda-bridge
 *
 * See scripts/oanda-bridge.ts for the replacement.
 */
console.error("[ibkr-bridge] This bridge has been replaced by the OANDA bridge.");
console.error("  Run: npm run oanda-bridge");
process.exit(1);
